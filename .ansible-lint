---
use_default_rules: true

skip_list:
  - role-name
  # must be fixed at later time
  - var-naming
  # temporary deactived - will be removed in next PRs
  - fqcn-builtins
  - schema

exclude_paths:
  - .github
  - dbhome-conversion
  - library
