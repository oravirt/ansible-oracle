---

attr_name: "{% if opdb.1.attributes is defined %}{{ opdb.1.attributes | default (omit) | map(attribute='name') | list }}
            {%- else %}None
            {%- endif %}"

attr_value: "{% if opdb.1.attributes is defined -%}{{ opdb.1.attributes | default (omit) | map(attribute='value') | list }}
             {%- else %}None{% endif %}"
